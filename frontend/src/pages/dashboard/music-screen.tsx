// import React from 'react'
import { motion } from 'framer-motion';
import { fadeAnimation } from '../../utils/motion';
// interface Props {}

const MusicScreen = () => {
  return <motion.div {...fadeAnimation}>MusicScreen</motion.div>;
};

export default MusicScreen;
