// import React from 'react'
import { motion } from 'framer-motion';
import { fadeAnimation } from '../../utils/motion';
// interface Props {}

const VideoScreen = () => {
  return <motion.div {...fadeAnimation}>VideoScreen</motion.div>;
};

export default VideoScreen;
